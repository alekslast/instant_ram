import 'package:flutter/material.dart';

class DirectPage extends StatelessWidget {
  const DirectPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Direct Page', style: TextStyle(color: Colors.white)),
    );
  }
}
